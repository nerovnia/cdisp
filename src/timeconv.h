// timeconv.h
#ifndef TIMECONV_H // include guard
#define TIMECONV_H

#include <string>

using namespace std;

string its_time(int i_time);
int sti_time(string s_time);
bool checkDigit(char digital);

#endif /* TIMECONV_H */
